In the data files provided, each row is a separate training example; the first 9 columns correspond to features, while the last column is the label (+1/-1). 

Important Note: the labels are of type double (+1/-1), unlike HW2.

Files:
cv_train.mat - Training instances + labels 480 x 10)
cv_test.mat - Training instances + labels (120 x 10)